"on":
  workflow_dispatch:
    inputs:
      target_env:
        required: true
        type: string
jobs:
  deploy:
    name: deploy
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - run: echo "deploying"
