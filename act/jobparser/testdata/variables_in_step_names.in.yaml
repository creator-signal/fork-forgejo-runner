name: test
env:
  NODE_VERSION: 22.x
jobs:
  job1:
    runs-on: linux
    steps:
      - name: Use Node.js ${{ env.NODE_VERSION }}
        run: echo "${{ env.NODE_VERSION }}"

      # Step with no name to ensure variable interpolation does not create empty step names.
      - run: |
          true
