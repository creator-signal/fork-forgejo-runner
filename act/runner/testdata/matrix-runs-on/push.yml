name: test

on: push

jobs:
  matrix-runs-on:
    strategy:
      matrix:
        os: [ubuntu-latest, ubuntu-20.04]
    runs-on: ${{ matrix.os }}
    steps:
      - run: |
          echo TESTOUTPUT: matrix.os == ${{ matrix.os }}
          echo TESTOUTPUT: runs-on == ${{ jobs.matrix-runs-on.runs-on }}

# BUG:
# [test/matrix-runs-on-2] 🚧  Skipping unsupported platform -- Try running with `-P ubuntu-20.04=...`
# [test/matrix-runs-on-1] 🚧  Skipping unsupported platform -- Try running with `-P ubuntu-20.04=...`
#
# Both jobs 1 and 2 are attempting to run with `ubuntu-20.04`!
# Also observed the opposite -- both jobs are run on ubuntu-latest!
